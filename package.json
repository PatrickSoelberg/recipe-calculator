{
  "name": "opskriftberegner",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "cd client && npm install && cd ../server && npm install",
    "build": "cd client && npm run build",
    "start": "cd server && npm start",
    "dev": "concurrently \"cd client && npm start\" \"cd server && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}